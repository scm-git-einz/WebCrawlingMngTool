# 웹 크롤링 에이전트 - 프롬프트 템플릿

> 이 문서는 Claude Code에서 웹 크롤링 에이전트를 개발할 때 사용하는 프롬프트 모음입니다.
> 대상 사이트의 URL만 변경하면 동일한 파이프라인을 다른 사이트에도 적용할 수 있습니다.

---

## 1단계: 크롤링 에이전트 초기 구축

> **용도**: 새로운 사이트에 대해 크롤링 에이전트를 처음 만들 때

```
당신은 웹 크롤링 에이전트입니다.
사이트의 정보를 크롤링하여 데이터를 수집하려고 합니다.
수집된 데이터는 json 형식으로 파일을 생성해주세요.
사이트의 구조를 보고 LLM을 호출하여 playwright를 사용할지 BeautifulSoup을 사용할지 판단을 해주세요.

수집을 위해 진행되는 프롬포트 내용은 마크다운하여 .md 파일로 남겨주세요.

1. 매장에 노출하고 있는 상품 정보를 수집합니다.
   user agent와 header를 추가하고 봇 감지를 우회하여 수집해야 합니다.
   실제 브라우저를 사용하여 접근하는 형태로 진행해야 합니다.
   다른 사이트의 정보는 수집하지 않아야 합니다.

2. 해당 매장에 포함된 상품 상세 정보를 수집합니다.
   상품 상세 정보에서 커머스에서 공통적으로 사용하는 element들은 수집해주세요.

노출 순서도 중요합니다.
API 키 없이 LLM 호출하여 진행해주세요.
크롤링하는 Agent가 개발되어야 합니다.

수집 대상 사이트입니다.
{여기에 대상 URL을 입력}
```

### 프롬프트 핵심 요소 설명

| 지시 | 역할 |
|------|------|
| `사이트의 구조를 보고 LLM을 호출하여 playwright를 사용할지 BeautifulSoup을 사용할지 판단` | 사이트 유형(SPA/정적)에 따라 도구 자동 선택 |
| `user agent와 header를 추가하고 봇 감지를 우회` | playwright-stealth, 실제 브라우저 헤더 적용 |
| `실제 브라우저를 사용하여 접근하는 형태` | headless 브라우저 + 사람처럼 행동하는 패턴 |
| `다른 사이트의 정보는 수집하지 않아야 합니다` | 도메인 필터링 적용 |
| `노출 순서도 중요합니다` | display_order 필드 생성 |
| `커머스에서 공통적으로 사용하는 element` | 가격, 브랜드, 이미지, 평점, 옵션, 재고 등 자동 추출 |
| `API 키 없이 LLM 호출하여 진행해주세요` | 외부 API 없이 Claude Code 자체 분석으로 진행 |
| `크롤링하는 Agent가 개발되어야 합니다` | 일회성 실행이 아닌 재실행 가능한 코드 개발 |
| `프롬포트 내용은 마크다운하여 .md 파일로 남겨주세요` | 프로세스 문서화 |

---

## 2단계: 기존 에이전트 수정/재실행

> **용도**: 이미 개발된 소스가 있고, 사이트 변경이나 차단 대응이 필요할 때

```
당신은 웹 크롤링 에이전트입니다.
사이트의 정보를 크롤링하여 데이터를 수집하려고 합니다.
수집된 데이터는 json 형식으로 파일을 생성해주세요.
사이트의 구조를 보고 LLM을 호출하여 playwright를 사용할지 BeautifulSoup을 사용할지 판단을 해주세요.

수집을 위해 진행되는 프롬포트 내용은 마크다운하여 .md 파일로 남겨주세요.
1. 매장에 노출하고 있는 상품 정보를 수집합니다.
user agent와 header를 추가하고 봇 감지를 우회하여 수집해야 합니다.
실제 브라우저를 사용하여 접근하는 형태로 진행해야 합니다.
다른 사이트의 정보는 수집하지 않아야 합니다.
현재 개발된 소스를 수정해서 다시 작성해주세요
```

### 1단계와의 차이점

| 차이 | 설명 |
|------|------|
| `현재 개발된 소스를 수정해서 다시 작성해주세요` | 기존 코드를 읽고 구조를 유지하면서 개선 |
| 상품 상세 관련 지시 생략 가능 | 이미 개발된 파서가 있으므로 목록 수집만 재지시 |

---

## 3단계: 차단 대응

> **용도**: 사이트에서 크롤링을 차단했을 때

```
{사이트명}에서 크롤링 차단한것 같아요.
```

> 이 한 줄이면 에이전트가 자동으로:
> 1. 현재 접속 상태 테스트 (여러 도메인/URL 패턴)
> 2. 차단 유형 분석 (WAF, CAPTCHA, IP 차단, 점검 페이지 등)
> 3. 우회 전략 제안 또는 자동 수정

---

## 4단계: 특정 요구사항 추가

> **용도**: 수집 범위나 조건을 변경할 때

### 상세 수집 개수 변경
```
상품 상세 수집 개수를 50개로 늘려주세요.
```

### 특정 카테고리만 수집
```
"스킨케어"와 "메이크업" 카테고리 상품만 수집해주세요.
```

### 수집 필드 추가
```
상품 상세에서 배송비 정보와 구매 후기 내용도 추가로 수집해주세요.
```

### 스케줄링
```
매일 오전 9시에 자동으로 크롤링이 실행되도록 스케줄을 설정해주세요.
```

---

## 5단계: 다른 사이트 적용

> **용도**: 동일한 구조로 다른 사이트 크롤링 에이전트를 만들 때

```
당신은 웹 크롤링 에이전트입니다.
사이트의 정보를 크롤링하여 데이터를 수집하려고 합니다.
수집된 데이터는 json 형식으로 파일을 생성해주세요.
사이트의 구조를 보고 LLM을 호출하여 playwright를 사용할지 BeautifulSoup을 사용할지 판단을 해주세요.

수집을 위해 진행되는 프롬포트 내용은 마크다운하여 .md 파일로 남겨주세요.

1. 매장에 노출하고 있는 상품 정보를 수집합니다.
   user agent와 header를 추가하고 봇 감지를 우회하여 수집해야 합니다.
   실제 브라우저를 사용하여 접근하는 형태로 진행해야 합니다.
   다른 사이트의 정보는 수집하지 않아야 합니다.

2. 해당 매장에 포함된 상품 상세 정보를 수집합니다.
   상품 상세 정보에서 커머스에서 공통적으로 사용하는 element들은 수집해주세요.

노출 순서도 중요합니다.
API 키 없이 LLM 호출하여 진행해주세요.
크롤링하는 Agent가 개발되어야 합니다.

D:\crawling 프로젝트의 구조를 참고하여 동일한 파이프라인으로 개발해주세요.
작업 디렉토리는 D:\crawling_{사이트코드} 로 생성해주세요.

수집 대상 사이트입니다.
{여기에 새 사이트 URL을 입력}
```

---

## 참고: 프롬프트 작성 시 주의사항

### 반드시 포함해야 하는 지시

1. **`크롤링하는 Agent가 개발되어야 합니다`**
   - 이 문장이 없으면 단순 fetch 후 결과만 보여주고 끝남
   - 재실행 가능한 Python 코드가 생성되도록 보장

2. **`user agent와 header를 추가하고 봇 감지를 우회`**
   - playwright-stealth 적용, 실제 브라우저 핑거프린트 설정
   - 이 지시 없으면 기본 Playwright로 접근하여 WAF에 차단됨

3. **`실제 브라우저를 사용하여 접근하는 형태`**
   - requests가 아닌 Playwright 브라우저 엔진 사용
   - SPA 사이트의 JavaScript 렌더링 처리

4. **`다른 사이트의 정보는 수집하지 않아야 합니다`**
   - 도메인 필터링 로직 생성
   - 외부 광고/트래커 차단

5. **`API 키 없이 LLM 호출하여 진행해주세요`**
   - 외부 Anthropic API 호출 없이 Claude Code 세션 내에서 분석
   - HTML 구조 분석, CSS 선택자 결정 등을 직접 수행

### 포함하면 좋은 선택적 지시

- `노출 순서도 중요합니다` - display_order 필드 생성
- `프롬포트 내용은 마크다운하여 .md 파일로 남겨주세요` - 프로세스 문서화
- `현재 개발된 소스를 수정해서 다시 작성해주세요` - 기존 코드 기반 수정
- `커머스에서 공통적으로 사용하는 element들은 수집` - 표준 커머스 필드 자동 추출

---

## 참고: 이 프로젝트에서 생성된 파일 구조

```
D:\crawling\
  browser.py               # Stealth 브라우저 설정 (봇 감지 우회)
  config.py                # URL, 딜레이, 타임아웃 설정
  fetch_ranking.py         # STEP 1: 페이지 HTML + AJAX 수집
  extract_data.py          # STEP 2: HTML 파싱 -> 상품 목록
  fetch_product_details.py # STEP 3: 상품 상세 수집
  generate_final.py        # STEP 4: 통합 JSON
  run.py                   # 통합 실행 (python run.py)
  requirements.txt         # 의존성
  CRAWLING_PROCESS.md      # 크롤링 프로세스 문서
  PROMPT_TEMPLATE.md       # 이 파일 (프롬프트 템플릿)
  output/
    02_shop_products.json  # 매장 + 상품 목록
    03_product_details.json # 상품 상세
    crawl_result.json      # 최종 통합 결과
```

---

## 참고: 수집되는 데이터 항목

### 상품 목록 (STEP 2)

| 필드 | 설명 |
|------|------|
| display_order | 매장 노출 순서 |
| product_id | 상품 고유 ID |
| product_name | 상품명 |
| brand | 브랜드명 |
| original_price | 정상가 |
| selling_price | 판매가 |
| discount_rate | 할인율 |
| krw_price | 원화 가격 |
| image_url | 상품 이미지 URL |
| product_url | 상세 페이지 URL |
| section | 카테고리/섹션명 |

### 상품 상세 (STEP 3)

| 필드 | 설명 |
|------|------|
| product_code | 상품 코드 |
| price (USD/KRW) | 정상가, 판매가, 최대혜택가 |
| rating | 평점, 리뷰 수 |
| images | 메인/썸네일/상세 이미지 |
| options | 색상, 사이즈 등 옵션 |
| stock_status | 판매중/품절 |
| description | 상품 설명 (500자) |
| coupons | 쿠폰/혜택 정보 |
